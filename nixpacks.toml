[phases.setup]
nixPkgs = [
    "python312",
    "pango",
    "gobject-introspection",
    "gdk-pixbuf",
    "cairo",
    "libffi",
    "gcc"
]

[phases.build]
cmds = ["python manage.py collectstatic --noinput"]

[start]
# v2 - Forzando actualizacion
cmd = "python manage.py migrate && python manage.py createsuperuser --noinput && gunicorn core.wsgi:application --bind 0.0.0.0:$PORT"